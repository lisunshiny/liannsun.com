name: Deploy

on:
  push:
    branches: [gh-pages]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install blog dependencies
        run: npm install
        working-directory: blog

      - name: Build blog
        run: npm run build
        working-directory: blog

      - name: Assemble site
        run: |
          mkdir -p _site/blog
          cp index.html styles.css script.js CNAME _site/
          cp -r assets bios fonts _site/
          cp -r blog/dist/. _site/blog/

      - uses: actions/upload-pages-artifact@v3
        with:
          path: _site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/deploy-pages@v4
        id: deployment
